import {UserModel} from './user.model';

export class UserService {
  public static getUser(id: number): UserModel {
    // Find the user. Return it
    return new UserModel;
  }

  public static updateUser(user: UserModel, update: UserModel) {
    // Find the user. Apply the update
    return;
  }
}
